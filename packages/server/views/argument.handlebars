<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="{{componentProps.resourcesRoot}}/fragments/global.css" type="text/css">
    <link rel="stylesheet" href="{{componentProps.resourcesRoot}}/argument.css" type="text/css">
    {{> fonts }}
    <script src="{{componentProps.resourcesRoot}}/argument.js" defer></script>
    <script id="argument-props" type="application/json">
      {{{json componentProps }}}
    </script>
    <title>{{ componentProps.initialArgument.conclusion }} - Wikisophia</title>
    <meta charset="UTF-8"/>
  </head>
  <body>
    <header>
      {{> header }}
    </header>
    <main>
      <!--
        This section must be kept in sync with the React component at
        <project-root>/packages/cdn/src/components/StaticArgument.jsx

        If javascript is enabled, React will purge the view-argument-anchor
        to make things editable.
      -->
      <section id="argument-anchor" class="main-content">
        <div class="argument-area">
          <h1 class="suppose">If you believe that</h1>
          <ul class="premises">
            {{#each componentProps.initialArgument.premises}}
              <div tabIndex="0" class="search control">s</div>
              <p class="premise">{{this}}</p>
            {{/each}}
          </ul>
          <h1 class="then">Then you should agree that</h1>
          <p class="conclusion">{{componentProps.initialArgument.conclusion}}</p>
        </div >
      </section>
    </main>
  </body>
</html>