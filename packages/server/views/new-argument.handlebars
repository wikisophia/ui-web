<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="{{resourcesRoot}}/global.css" type="text/css">
    <link rel="stylesheet" href="{{resourcesRoot}}/argument-area.css" type="text/css">
    {{> fonts }}
    <script src="{{resourcesRoot}}/new-argument.js" defer></script>
    <script id="argument-props" type="application/json">
      {{{json componentProps }}}
    </script>
    <title>New Argument - Wikisophia</title>
    <meta charset="UTF-8"/>
  </head>
  <body>
    <header>
      {{> header }}
    </header>
    <main>
      <!--
        This section must be kept in sync with the React component at
        <project-root>/packages/cdn/src/components/ImprovingArgument.jsx

        Otherwise users will see a flicker on page load.
      -->
      <section id="argument-anchor" class="main-content">
        <div class="argument-area hidden">
          <h1 class="suppose">The belief that</h1>
          <div class="conclusion-area">
            <input class="conclusion editing" type="text" value="{{componentProps.initialConclusion}}">
          </div>
          <h1 class="then">Is reasonable if</h1>
          <ul class="premises">
            {{#each componentProps.initialPremises}}
              <input class="premise" type="text" value="{{this}}">
            {{/each}}
            <div class="new control" tabindex="0">new</div>
          </ul>
        </div>
        <noscript>
          Javascript must be enabled to submit new arguments.
        </noscript>
      </section>
    </main>
  </body>
</html>