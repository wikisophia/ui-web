<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="{{resourcesRoot}}/global.css" type="text/css">
    <link rel="stylesheet" href="{{resourcesRoot}}/argument-area.css" type="text/css">
    {{> fonts }}
    <title>{{ conclusion }} - Wikisophia</title>
    <meta charset="UTF-8"/>
  </head>
  <body>
    <header>
      {{> header }}
    </header>
    <main>
      <!--
        This section must be kept in sync with the React component at
        <project-root>/packages/cdn/src/components/StaticArgument.jsx

        If javascript is enabled, React will purge the argument-anchor
        to make things editable.
      -->
      <section class="main-content">
        <p class="outdated">
          <span>This argument may be out of date. View the <a href="/arguments/{{id}}">latest version</a><span>.</span>
        </p>
        <div class="argument-area">
          <h1 class="suppose">The belief that</h1>
          <div class="conclusion-area">
            <p class="conclusion">{{conclusion}}</p>
          </div>
          <h1 class="then">Is reasonable if</h1>
          <ul class="premises">
            {{#each premises}}
              {{#if this.supported}}
                <a class="premise justified" href="/arguments?conclusion={{this.text}}">{{this.text}}</a>
              {{else}}
                <a class="premise unjustified" href="/arguments?conclusion={{this.text}}">{{this.text}}</a>
              {{/if}}
            {{/each}}
          </ul>
        </div >
      </section>
    </main>
  </body>
</html>